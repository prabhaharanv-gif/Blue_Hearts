{
  "name": "blue-hearts",
  "version": "1.0.0",
  "description": "Private two-person chat. Name-only login, nothing stored.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.1"
  },
  "engines": {
    "node": ">=20"
  }
}
